---
apiVersion: v1
kind: Service
metadata:
  name: vectorize-agent-service
  namespace: euler-copilot
spec:
  ports:
  - name: httpport
    protocol: TCP
    port: 8001
    targetPort: 8001
  selector:
    app: vectorize-agent
  type: LoadBalancer
