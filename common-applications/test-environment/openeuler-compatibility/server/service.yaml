apiVersion: v1
kind: Service
metadata:
  name: server-service
  namespace: openeuler-compatibility
spec:
  ports:
  - name: http-port
    port: 8003
    protocol: TCP
    targetPort: 8003
  selector:
    app: server
  type: ClusterIP