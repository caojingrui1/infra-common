apiVersion: v1
kind: Service
metadata:
  name: flexcompute-service
  namespace: merlin
spec:
  ports:
  - name: http-port
    port: 8125
    protocol: TCP
    targetPort: 8125
  selector:
    app: flexcompute
  type: ClusterIP