apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uptime-kuma-pvc-0
  namespace: merlin
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100G
  storageClassName: csi-disk
