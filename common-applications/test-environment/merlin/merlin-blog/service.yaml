apiVersion: v1
kind: Service
metadata:
  name: merlin-blog-service
  namespace: merlin
spec:
  ports:
  - name: https-port
    protocol: TCP
    port: 8080
    targetPort: 8080
  selector:
    app: merlin-blog
  type: ClusterIP

  
