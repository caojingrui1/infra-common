apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cla-data-vol
  namespace: cla
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: csi-disk-sas
  volumeMode: Filesystem
