kind: NetworkPolicy
metadata:
  name: modelfoundry-usercenter-website-policy-01
  namespace: modelfoundry-usercenter-website
spec:
  podSelector:
    matchLabels:
      app: website
  ingress:
    - ports:
        - protocol: TCP
          port: 8080
      from:
        - namespaceSelector:
            matchLabels:
              project: ingress-nginx
  policyTypes:
    - Ingress
apiVersion: networking.k8s.io/v1
