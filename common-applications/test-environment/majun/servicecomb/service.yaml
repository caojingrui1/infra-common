apiVersion: v1
kind: Service
metadata:
  name: majun-servicecomb
  namespace: majun
spec:
  ports:
  - name: http-port
    port: 30100
    protocol: TCP
    targetPort: 30100
  selector:
    app: servicecomb
  type: ClusterIP