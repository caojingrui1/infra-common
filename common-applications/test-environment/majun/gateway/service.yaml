apiVersion: v1
kind: Service
metadata:
  name: majun-gateway
  namespace: majun
spec:
  ports:
  - name: http-port
    port: 8089
    protocol: TCP
    targetPort: 8089
  selector:
    app: gateway
  type: ClusterIP