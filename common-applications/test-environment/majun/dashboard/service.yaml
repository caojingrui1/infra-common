apiVersion: v1
kind: Service
metadata:
  name: majun-dashboard
  namespace: majun
spec:
  ports:
  - name: http-port
    port: 8093
    protocol: TCP
    targetPort: 8093
  selector:
    app: dashboard
  type: ClusterIP