apiVersion: v1
kind: Service
metadata:
  name: majun-platform-release
  namespace: majun
spec:
  ports:
  - name: http-port
    port: 8085
    protocol: TCP
    targetPort: 8085
  selector:
    app: platform-release
  type: ClusterIP