apiVersion: v1
kind: Service
metadata:
  name: majun-basic
  namespace: majun
spec:
  ports:
  - name: http-port
    port: 8077
    protocol: TCP
    targetPort: 8077
  selector:
    app: basic
  type: ClusterIP