---
apiVersion: v1
kind: Service
metadata:
  name: app-publish-service
  namespace: majun
spec:
  ports:
  - name: httpport
    protocol: TCP
    port: 8080
    targetPort: 8080
  selector:
    app: post-publish
  type: ClusterIP