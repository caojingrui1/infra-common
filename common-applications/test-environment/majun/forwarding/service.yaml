apiVersion: v1
kind: Service
metadata:
  name: majun-forwarding
  namespace: majun
spec:
  ports:
  - name: http-port
    port: 8083
    protocol: TCP
    targetPort: 8083
  selector:
    app: forwarding
  type: ClusterIP