apiVersion: v1
kind: Service
metadata:
  name: majun-manager
  namespace: majun
spec:
  ports:
  - name: http-port
    port: 8023
    protocol: TCP
    targetPort: 8023
  selector:
    app: manager
  type: ClusterIP