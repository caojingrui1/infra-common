---
# Service
apiVersion: v1
kind: Service
metadata:
  name: sca-web
  namespace: sca
  labels:
    app: sca-web
spec:
  type: ClusterIP
  ports:
    - port: 8077
      name: web-app
      protocol: TCP
      targetPort: 8077
  selector:
    app: sca-web