apiVersion: v1
kind: Service
metadata:
  name: bisect-manager-service
  namespace: openeuler-devops
spec:
  ports:
  - name: http-port
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: bisect-manager
  type: ClusterIP
