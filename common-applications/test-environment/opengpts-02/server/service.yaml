apiVersion: v1
kind: Service
metadata:
  name: server-service
  namespace: opengpts-02
spec:
  ports:
  - name: http-port
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: server
  type: ClusterIP
