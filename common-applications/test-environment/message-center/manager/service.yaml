apiVersion: v1
kind: Service
metadata:
  name: manager-service
  namespace: message-center
spec:
  ports:
  - name: http-port
    port: 8082
    protocol: TCP
    targetPort: 8082
  selector:
    app: manager
  type: ClusterIP
