apiVersion: v1
kind: Service
metadata:
  name: collect-githook-service
  namespace: message-center
spec:
  ports:
  - name: http-port
    port: 8888
    protocol: TCP
    targetPort: 8888
  selector:
    app: collect-githook
  type: ClusterIP
