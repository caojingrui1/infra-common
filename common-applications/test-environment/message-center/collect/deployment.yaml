apiVersion: apps/v1
kind: Deployment
metadata:
  name: collect-deployment
  namespace: message-center
spec:
  selector:
    matchLabels:
      app: collect
  strategy:
    rollingUpdate:
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      annotations:
        vault.hashicorp.com/agent-inject: 'true'
        vault.hashicorp.com/agent-pre-populate-only: "true"
        vault.hashicorp.com/tls-skip-verify: "true"
        vault.hashicorp.com/agent-service-account-token-volume-name: "token-vol"
        vault.hashicorp.com/agent-run-as-user: "1000"
        vault.hashicorp.com/agent-run-as-group: "1000"
        vault.hashicorp.com/role: 'message-center'
        vault.hashicorp.com/agent-inject-perms-conf.yaml: "0400"
        vault.hashicorp.com/agent-inject-secret-conf.yaml: 'internal/data/infra-test/message-center'
        vault.hashicorp.com/agent-inject-template-conf.yaml: |  
          {{- with secret "internal/data/infra-test/message-center" -}}  
          {{ .Data.data.collectConf }}
          {{- end }}
        vault.hashicorp.com/agent-inject-perms-eur_build_conf.yaml: "0400"
        vault.hashicorp.com/agent-inject-secret-eur_build_conf.yaml: 'internal/data/infra-test/message-center'
        vault.hashicorp.com/agent-inject-template-eur_build_conf.yaml: |  
          {{- with secret "internal/data/infra-test/message-center" -}}  
          {{ .Data.data.collectEurBuildConf }}
          {{- end }}
        vault.hashicorp.com/agent-inject-perms-meeting_conf.yaml: "0400"
        vault.hashicorp.com/agent-inject-secret-meeting_conf.yaml: 'internal/data/infra-test/message-center'
        vault.hashicorp.com/agent-inject-template-meeting_conf.yaml: |  
          {{- with secret "internal/data/infra-test/message-center" -}}  
          {{ .Data.data.collectMeetingConf }}
          {{- end }}
        vault.hashicorp.com/agent-inject-perms-kafka.crt: "0400"
        vault.hashicorp.com/agent-inject-secret-kafka.crt: 'internal/data/infra-test/message-center'
        vault.hashicorp.com/agent-inject-template-kafka.crt: |  
          {{- with secret "internal/data/infra-test/message-center" -}}  
          {{ .Data.data.kafkaCrt }}
          {{- end }}
        vault.hashicorp.com/agent-inject-perms-kafka-cn4.crt: "0400"
        vault.hashicorp.com/agent-inject-secret-kafka-cn4.crt: 'internal/data/infra-test/message-center'
        vault.hashicorp.com/agent-inject-template-kafka-cn4.crt: |  
          {{- with secret "internal/data/infra-test/message-center" -}}  
          {{ .Data.data.kafkaCn4Crt }}
          {{- end }}
      labels:
        app: collect
    spec:
      serviceAccount: message-center
      imagePullSecrets:
      - name: huawei-swr-image-pull-secret
      automountServiceAccountToken: false
      containers:
      - name: server
        args:
        - --config-file=/vault/secrets/conf.yaml
        - --eur-build-config-file=/vault/secrets/eur_build_conf.yaml
        - --meeting-config-file=/vault/secrets/meeting_conf.yaml
        image: swr.cn-north-4.myhuaweicloud.com/opensourceway/message-center/message-collect-test:ssp_dev-ad1f82
        imagePullPolicy: IfNotPresent
        resources:
          limits:
            cpu: 2
            memory: 8Gi
          requests:
            cpu: 500m
            memory: 500Mi
        securityContext:
          allowPrivilegeEscalation: false
          runAsUser: 1000
          readOnlyRootFilesystem: true
          capabilities:
            drop:
              - ALL
      volumes:
      - name: token-vol
        projected:
          sources:
          - serviceAccountToken:
              audience: api
              expirationSeconds: 600
              path: token
