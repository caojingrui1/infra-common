apiVersion: v1
kind: Service
metadata:
  name: flexcompute-service
  namespace: merlin-02
spec:
  ports:
  - name: http-port
    port: 8125
    protocol: TCP
    targetPort: 8125
  selector:
    app: flexcompute
  type: ClusterIP