apiVersion: v1
kind: Service
metadata:
  name: space-service
  namespace: merlin-02
spec:
  ports:
  - name: http-port
    port: 8123
    protocol: TCP
    targetPort: 8123
  selector:
    app: space
  type: ClusterIP
