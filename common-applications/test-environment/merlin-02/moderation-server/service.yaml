apiVersion: v1
kind: Service
metadata:
  name: moderation-server-service
  namespace: merlin-02
spec:
  ports:
  - name: http-port
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: moderation-server
  type: ClusterIP
