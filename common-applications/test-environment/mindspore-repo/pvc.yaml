apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mindspore-repo-volume
  namespace: mindspore-repo
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: csi-disk
  volumeMode: Filesystem