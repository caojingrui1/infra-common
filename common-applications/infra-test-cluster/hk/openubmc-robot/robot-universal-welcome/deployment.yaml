apiVersion: apps/v1
kind: Deployment
metadata:
  name: robot-universal-welcome-deployment
  namespace: robot-openubmc
  labels:
    app: robot-universal-welcome
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: robot-universal-welcome
  template:
    metadata:
      annotations:
        vault.hashicorp.com/agent-inject: 'true'
        vault.hashicorp.com/agent-pre-populate-only: "true"
        vault.hashicorp.com/tls-skip-verify: "true"
        vault.hashicorp.com/agent-init-first: "true"
        vault.hashicorp.com/role: 'robot-openubmc'
        vault.hashicorp.com/agent-service-account-token-volume-name: "token-vol"
        vault.hashicorp.com/agent-run-as-user: "1000"
        vault.hashicorp.com/agent-run-as-group: "1000"
        vault.hashicorp.com/agent-inject-perms-config: "0400"
        vault.hashicorp.com/agent-inject-secret-config: 'internal/data/infra-test/robot-universal-welcome'
        vault.hashicorp.com/agent-inject-template-config: |  
          {{- with secret "internal/data/infra-test/robot-universal-welcome" -}}  
          {{ .Data.data.config }}
          {{- end }}
        vault.hashicorp.com/agent-inject-perms-token: "0400"
        vault.hashicorp.com/agent-inject-secret-token: 'internal/data/infra-test/robot-universal-welcome'
        vault.hashicorp.com/agent-inject-template-token: |  
          {{- with secret "internal/data/infra-test/robot-universal-welcome" -}}  
          {{ .Data.data.token }}
          {{- end }}
      labels:
        app: robot-universal-welcome
    spec:
      securityContext:
        seccompProfile:
          type: Localhost
          localhostProfile: infra-seccomp.json
      automountServiceAccountToken: false
      serviceAccountName: robot-openubmc
      imagePullSecrets:
      - name: huawei-swr-image-pull-secret
      containers:
      - name: robot-universal-welcome
        args:
        - --port=8888
        - --handle-path=gitcode-hook
        image: swr.cn-north-4.myhuaweicloud.com/opensourceway/robot/robot-universal-welcome:develop-v0-6dbdce
        resources:
          limits:
            cpu: 1
            memory: 1Gi
          requests:
            cpu: 500m
            memory: 1Gi
        livenessProbe:
          failureThreshold: 3
          initialDelaySeconds: 10
          periodSeconds: 10
          successThreshold: 1
          httpGet:
            scheme: HTTP
            path: /
            port: 8888
          timeoutSeconds: 5
        readinessProbe:
          failureThreshold: 3
          initialDelaySeconds: 10
          periodSeconds: 10
          successThreshold: 1
          httpGet:
            scheme: HTTP
            path: /
            port: 8888
          timeoutSeconds: 5
        securityContext:
          allowPrivilegeEscalation: false
          runAsUser: 1000
          capabilities:
            drop:
              - ALL
      volumes:
        - name: token-vol
          projected:
            sources:
            - serviceAccountToken:
                audience: api
                expirationSeconds: 600
                path: token