apiVersion: v1
kind: Service
metadata:
  name: server-service
  namespace: mindspore-easyeditor
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: mindspore-easyeditor
  type: ClusterIP
