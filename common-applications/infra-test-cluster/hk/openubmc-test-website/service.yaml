apiVersion: v1
kind: Service
metadata:
  name: openubmc
  namespace: openubmc
spec:
  ports:
  - name: http-port
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: openubmc-website
  type: ClusterIP
