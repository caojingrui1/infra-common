apiVersion: v1
kind: Service
metadata:
  name: openubmc
  namespace: openubmc
spec:
  ports:
  - name: http-port
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: openubmc-website
  type: ClusterIP
