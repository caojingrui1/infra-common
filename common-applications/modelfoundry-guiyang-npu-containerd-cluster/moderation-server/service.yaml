apiVersion: v1
kind: Service
metadata:
  name: moderation-server-service
  namespace: moderation-server
spec:
  ports:
  - name: https-port
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: moderation-server
  type: ClusterIP
